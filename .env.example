# Copy to .env before running: cp .env.example .env
# Name: Johan Tom Chacko
# Date: 2026-05-29
# What this file does: example settings for local/docker runs. Safe to commit — no passwords or API keys needed for this project.

DATABASE_URL=sqlite:///./data/weather.db
POLL_INTERVAL_SECONDS=300
ENABLE_POLLER=true
